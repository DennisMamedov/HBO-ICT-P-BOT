{
  "dependencies": {
    "discord.js": "^12.3.1",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^14.6.2",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "start": "node dist/app.js",
    "dev": "yarn build && yarn start"
  }
}
